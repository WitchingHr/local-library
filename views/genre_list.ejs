<!DOCTYPE html>
<html>
  <%- include('./partials/head.ejs') %>
  <body>
    <div class='container-fluid'>
      <div class='row'>
        <%- include('./partials/nav.ejs') %>
        <div class="col-sm-10">
          <h1><%= title %></h1>

          <ul>
            <% for (var i = 0; i < genres.length; i++) { %>
              <li><a href="/catalog/genre/<%= genres[i]._id %>"><%= genres[i].name %></a></li>
            <% } %>
          </ul>

        </div>
      </div>
    </div>
  </body>
</html>